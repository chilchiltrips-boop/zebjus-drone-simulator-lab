# ZEBJUS Flight Lab V18.3.40

- Python Lab IMU Live Graph and Run / Terminal are now separate cards stacked vertically.
- Terminal is intentionally shorter so live sensor graphs keep more usable space.
- Added **Copy Output** to copy the complete Python terminal result with Clipboard API + fallback.
- Terminal remains bounded and scrollable; long output no longer stretches the page.
- Existing IMU live rate/age, CSV recording, browser calibration and Python Worker Stop behavior are preserved.
