ZEBJUS V18.3.46 - COMPLETE GITHUB ROOT UPLOAD / REPLACE-IN-PLACE

Upload the CONTENTS of this project folder into the ROOT of the GitHub repository.
index.html must remain at repository root.

macOS:
- Press Cmd + Shift + . to show the hidden .github folder before selecting files.
- Select all project files/folders and drag them into the repository root.
- When GitHub shows same-name files, the new files replace/update the old files.

IMPORTANT STABLE NAMES:
- FlightCore_Firmware/ZEBJUS_FLIGHTCORE.ino
- FlightCore_Firmware/ZEBJUS_FLIGHTCORE_A1_APP.bin   (generated by GitHub Actions)
- FlightCore_Firmware/ZEBJUS_FLIGHTCORE_A2_APP.bin   (generated by GitHub Actions)
- firmware-catalog.json
- firmware-latest.json
- app.js / styles.css / index.html / service-worker.js

Do NOT rename the .ino with a version number in future releases.
When ZEBJUS_FLIGHTCORE.ino is replaced, .github/workflows/build-flightcore-a1.yml builds A1 and A2 images and updates the board catalog. If GitHub requires two upload groups, finish both groups and rerun the workflow manually after the assets are present.

Current build framework: Arduino-ESP32 3.3.12.
Integrity check: python3 tools/validate_project.py

Historical RELEASE_NOTES_V*.md files are intentionally version-named and may remain together.
